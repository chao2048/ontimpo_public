<p style="color: green"><%= notice %></p>

<header>
  <nav class="navbar">
    <h1>Events</h1>
  </nav>
</header>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
</div>

<%= link_to "New event", new_event_path %> |
<%= link_to "Back to Calendar", timetable_path %>
